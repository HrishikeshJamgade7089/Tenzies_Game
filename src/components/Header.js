import React from "react";

function Header(){
    return(
        <div className="heading_line">
            <h1 className="heading_line_child1">Tenzies</h1>
            <p className='heading_line_child2'>Roll until all dice are the same. Click each die to freeze it at its current value between rolls.</p>
        </div>
    )
}

export default Header;